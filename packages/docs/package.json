{
  "name": "docs",
  "type": "module",
  "private": true,
  "scripts": {
    "generate-reference": "pnpm -r generate-reference && python3 generate-reference.py",
    "build": "pnpm generate-reference && astro build",
    "dev": "pnpm generate-reference && astro dev"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.38.3",
    "astro": "^6.1.5",
    "sharp": "^0.34.5"
  }
}
