{
  "private": true,
  "packageManager": "pnpm@8.15.9",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "check": "pnpm -r check",
    "format": "pnpm -r format",
    "tsc": "pnpm -r tsc",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.4",
    "typescript": "5"
  }
}
