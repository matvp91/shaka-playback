{
  "name": "bap",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "format": "pnpm -r format",
    "tsc": "pnpm -r tsc"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.4",
    "typescript": "5"
  }
}
