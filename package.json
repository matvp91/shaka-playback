{
  "version": "0.0.1",
  "private": true,
  "packageManager": "pnpm@8.15.9",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "format": "pnpm -r format",
    "tsc": "pnpm -r tsc"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.4",
    "typescript": "5"
  }
}
